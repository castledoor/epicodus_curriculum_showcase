// describe('factorOne', function() {
//   it("Factors only one number", function() {
//     factorOne(7).should.equal(42);
//  });
// });

describe('factor', function() {
  it("Factors the number", function() {
    factor(7).should.equal(5040);
  }); 
});